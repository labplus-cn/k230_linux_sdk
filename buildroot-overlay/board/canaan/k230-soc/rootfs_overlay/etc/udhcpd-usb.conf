# USB 虚拟网卡 (RNDIS) DHCP 服务: PC 端即插即用自动获取 IP
# 由 cdc_serial.sh 的 net_up 启动, 板上固定 IP 10.0.10.1
interface usb0
start 10.0.10.100
end 10.0.10.200
max_leases 10
opt subnet 255.255.255.0
opt router 10.0.10.1
opt lease 86400
pidfile /var/run/udhcpd-usb.pid
lease_file /var/lib/misc/udhcpd.leases
